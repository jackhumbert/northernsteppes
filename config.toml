# The URL the site will be built for
base_url = "https://northernsteppes.com"
title = "The Northern Steppes"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = false

[extra]

# 0 == Sunday

[[extra.practices]]
location_name = "Shoaff Park by the Splash Pad"
location_coords = "41.144078,-85.097697"
day_of_the_week = 0
time_of_day = [13, 0]
length = 4

[[extra.practices]]
location_name = "East Swinney Park by the Disc Golf"
location_coords = "41.073488,-85.160017"
day_of_the_week = 5
time_of_day = [18, 0]
length = 3
